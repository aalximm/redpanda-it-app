import { LinkProps } from './Link.props';
import styles from './Link.module.css';
import cn from 'classnames';

export const H = ({ className, children, ...props }: LinkProps) => {
	return (
		<>
		</>
	);
}